# CLAUDE.md — 이 프로젝트를 고치는 클로드에게

카카오톡 자동 메시지 발송 매크로 (macOS 전용, Apple Silicon).
**사용자는 개발 지식이 없는 사람**이고, 코드 수정은 사실상 전부 클로드가 한다는 전제로 쓰여 있다.

먼저 읽어야 할 것: [실측으로 확인한 사실들](#실측으로-확인한-사실들) — 이걸 모르고 고치면
이미 겪은 사고를 그대로 반복한다. 특히 이미지 전송과 코드 서명.

---

## 대전제

1. **사용자에게 터미널을 시키지 마라.** 해결책은 앱 안에 있어야 한다. 문서로 가르치는 것보다
   기능이 대신하는 게 낫다.
2. **실패를 조용히 삼키지 마라.** 모든 실패는 화면 로그(`self.signals.log.emit`) 또는
   상세 로그(`dlog`)에 남아야 한다. 이 프로젝트에서 가장 많은 시간을 잡아먹은 게
   "실패했는데 아무 말이 없어서 원인을 못 찾는" 상황이었다.
3. **잘못된 성공 보고를 하지 마라.** 이미지를 한 장도 못 보냈는데 "성공 (이미지 3장 포함)"이라고
   찍던 버그가 있었다. 실제로 한 일만 보고해야 한다.
4. **타이밍을 추측으로 조정하지 마라.** `time.sleep` 값을 줄이면 다른 기기에서 깨진다.
   기다려야 한다면 고정 시간이 아니라 **상태를 확인하며** 기다려라.

---

## 구조

| 파일 | 역할 |
|---|---|
| `gui.py` | PyQt6 GUI + 발송 로직 전체. **진입점이자 사실상의 본체** |
| `kakao_controller.py` | 카카오톡 상태 읽기 (접근성/AX + AppleScript). 창 제목, 친구 목록, 마지막 메시지, 전송 확인 창 감지 |
| `input_controller.py` | 키보드/마우스/클립보드 (AppleScript `key code` + pyautogui + NSPasteboard) |
| `csv_reader.py` | CSV 파싱. 헤더 별칭, 이미지 `;` 구분, 제외 행 리포트 |
| `debug_log.py` | 진단용 상세 로그 링버퍼 (스레드 안전, 최근 3000줄) |
| `logger.py`, `main.py` | CLI 버전용. **GUI와 로직이 중복되어 있다** (아래 알려진 빚 참고) |
| `version.py` | 버전 단일 소스. 창 제목·Info.plist·DMG 파일명에 자동 반영 |
| `build.sh` | 빌드 → 서명 검증 → 기동 스모크 테스트 → DMG 생성 |
| `설치하기.command` | DMG에 동봉. 앱 복사 + 격리 해제 + 설정 창 열기 |
| `클로드와 함께 설치하기.txt` | DMG에 동봉. 설치 막힌 사용자가 통째로 클로드에게 붙여넣는 안내문 |

발송 흐름 (`gui.py`의 `_send_messages` → `_find_and_verify_chat` → `_send_and_verify`):

```
CSV 로드 → 한 명씩:
  메인 창 활성화 → Cmd+F 검색 → 이름 붙여넣기 → ↓↓ Enter로 채팅방 열기
  → 창 제목이 이름과 정확히 일치할 때까지 다음 검색 결과로 이동 (수신자 검증)
  → 입력창 클릭 → 포커스 확인 → 이미지 전송 → 메시지 전송 → 검증 → Cmd+W
```

---

## 실측으로 확인한 사실들

이 절의 내용은 전부 **실제 카카오톡을 관측해서 확인한 것**이다. 추측이 아니다.
반대되는 코드를 짜기 전에 반드시 다시 실측해라.

### 이미지 전송 확인 창 = "접근성 소등"이 신호다

여러 장 이미지를 파일 URL로 붙여넣으면(`paste_files`) 카카오톡이 전송 확인 창을 띄운다.
이 창은 **접근성 트리 어디에도 나타나지 않는다.**

- `AXWindows`에 새 창으로 안 잡힌다. `AXSheets`도 아니다. `AXSubrole`이 다이얼로그도 아니다.
- CGWindowList(윈도우 서버)에도 별도 창으로 안 잡힌다.
- 대신 **카카오톡의 접근성 트리 전체가 응답하지 않는다.** `AXWindows`가 빈 값이 되고,
  System Events는 `-1719 (유효하지 않은 인덱스)` 오류를 낸다.

즉 **창이 늘어나는 게 아니라 창 목록이 사라지는 것**이 확인 창이 떴다는 신호다.
(`kakao_controller.is_send_dialog_up()`)

**소등 전에 Enter를 누르면 준비 중이던 첨부가 취소된다.** 그러면 확인 창은 영영 안 뜨고
이미지가 하나도 안 간다. 소등된 뒤의 Enter는 안전하다 (로딩 중이면 무시, 준비됐으면 전송,
이미 닫혔으면 빈 입력창이라 무해).

전송이 끝난 뒤에도 소등이 **8~40초** 유지될 수 있다. 그래서 "점등 = 전송 완료"로 쓰면 안 된다.

이 사실을 모르고 만든 버전들이 낸 사고:

| 버전 | 접근 | 결과 |
|---|---|---|
| v1.2.4 | 시트/다이얼로그 서브롤로 감지 | 감지 실패 → 폴백의 첫 Enter가 묶음 창을 눌러 **중복 전송** |
| v1.2.5 | 창 목록 스냅샷 비교 | 역시 감지 실패. 폴링이 우연히 6초 딜레이를 만들어 "가긴 갔다" |
| v1.2.5 특 | 감지 없이 2.1초 후 Enter | **첨부 취소** → 이미지 아예 안 감 |
| v1.2.7 | 1.5초 간격 Enter 반복 | 마찬가지로 첨부 취소 |
| v1.2.9 | **소등 감지** | 정상. 기기 속도와 무관 |

### 친구 목록 접근성 구조

- 메인 창 "카카오톡" → `AXScrollArea` → `AXOutline` → `AXRows`
- `AXDisclosureLevel` 0 = 섹션 헤더, 1 = 항목. **"친구" 헤더 다음의 레벨 1 행들만** 실제 친구다.
  (즐겨찾기 섹션을 안 거르면 중복된다)
- 셀 자식 순서가 행 종류마다 다르다. 친구 행 = [프로필사진 `AXButton`, 이름, 상태메시지],
  헤더 행 = [이름, 개수 배지, 버튼]. 헤더에서 두 번째를 집으면 인원수를 이름으로 착각한다.
- **성능**: 행당 개별 AX 호출은 ~4.7ms(4천 명이면 19초). `AXUIElementCopyMultipleAttributeValues`
  배치 호출로 ~2.7ms까지 준다. AppleScript 통짜 조회는 오히려 더 느리다. 병렬화는 효과 없다
  (카카오톡이 AX 요청을 직렬 처리한다).
- 대형 목록에서는 `AXUIElementSetMessagingTimeout`을 넉넉히(30초) 주지 않으면
  개별 호출이 조용히 실패한다.

### 코드 서명 — 하지 말아야 할 것

- **`codesign --deep`은 절대 쓰지 마라.** 이 번들 구조에서 Bus error(138)로 크래시하며
  반쯤 쓰다 만 서명(`.cstemp` 잔여물)을 남겨 앱을 손상시킨다. v1.2.0에서 사용자들이 겪은
  "손상되었기 때문에 열 수 없습니다"의 원인이 설치 스크립트의 이 명령이었다.
- **번들 전체 봉인(`codesign --force --sign - 앱.app`)도 하지 마라.** 메인 실행 파일을
  자기 자신의 봉인 목록에 포함하는 모순이 생겨 만들자마자 깨진다.
- PyInstaller가 각 바이너리에 넣는 ad-hoc 서명 + 격리 해제(`xattr -cr`)면 정상 실행된다.
  **설치 시에 필요한 건 격리 해제뿐이다.**
- 빌드할 때마다 서명이 달라져서 **손쉬운 사용 권한이 토글은 켜진 채 무효**가 된다.
  사용자는 `ㅡ`로 제거 후 `+`로 재추가해야 한다. 앱이 `AXIsProcessTrusted()`로 이걸 감지해서
  안내한다 (`gui.App._check_accessibility`).
- 설치 스크립트에서 `lipo` 같은 개발자 도구를 쓰면 "명령어 라인 개발자 도구를 설치하시겠습니까?"
  팝업이 떠서 비개발자를 혼란시킨다. CPU 확인은 `sysctl -n hw.optional.arm64`로 한다.

---

## 절대 건드리면 안 되는 것

1. **수신자 검증** (`_find_and_verify_chat`) — 채팅창 제목이 대상 이름과 **정확히 일치**할
   때까지 다음 검색 결과로 넘어가는 로직. 부분 일치나 유사도 매칭으로 "개선"하면
   **엉뚱한 사람에게 메시지가 간다.** 이 프로그램에서 가장 위험한 사고다.
2. **소등 전 키 입력 금지** — 위 실측 참고. `_confirm_send_dialog`가 확인 창을 기다리는
   동안에는 어떤 키도 보내면 안 된다. 읽기(AX 조회)는 괜찮다.
3. **`time.sleep` 값 줄이기** — 특히 이미지 로드·전송 대기. 이 맥에서 빨라 보여도
   느린 기기에서 깨진다. 줄여야 한다면 고정 시간을 없애고 상태 확인으로 바꿔라.
4. **빌드 후 `Info.plist` 수정** — PlistBuddy로 고치면 서명이 깨진다. 버전·권한 설명은
   `카카오톡매크로.spec`의 `BUNDLE info_plist`에서 빌드 시점에 주입된다.

---

## 테스트하는 법

실제 카카오톡이 필요해서 자동 테스트가 안 된다. **본인 계정으로 검증**하는 게 표준 절차다.

```bash
python3 gui.py    # 설치 없이 소스로 바로 실행. 반복 테스트는 이게 제일 빠르다
```

1. 카카오톡에서 **나와의 채팅** 또는 본인이 만든 테스트 방을 연다.
2. 그 방 이름으로 1행짜리 CSV를 만든다. 이미지 여러 장 케이스를 꼭 포함한다.
3. 발송하고, **화면 로그와 실제 채팅방을 눈으로 대조**한다.
   로그가 "성공"이라고 해도 실제로 갔는지는 별개다 (실제로 그런 버그가 있었다).
4. 이미지 관련 변경이라면 1장 / 여러 장 / 파일 없는 경로를 모두 확인한다.

빌드 검증은 `./build.sh`가 기동 스모크 테스트까지 한다 (앱 창이 3초 떴다 사라진다).
DMG를 만들면 마운트해서 설치 시뮬레이션(복사 → `xattr -cr` → 실행)까지 해보는 게 좋다.

---

## 배포

```bash
# 1. version.py 수정 (예: 1.2.9 → 1.3.0)
# 2. 빌드
./build.sh                    # dist/카카오톡매크로_설치_v1.3.0.dmg
# 3. 검증 후 태그
git tag v1.3.0 && git push --tags
```

태그를 푸시하면 GitHub Actions(`.github/workflows/release.yml`)가 macOS 러너에서
DMG를 빌드해 Releases에 자동으로 첨부한다. **태그와 `version.py`가 다르면 실패**한다.

평소 커밋·PR 머지로는 릴리즈가 만들어지지 않는다. 태그를 찍는 것이 "배포한다"는 의사표시다.

---

## 알려진 빚

고쳐도 되는 것들. 여유가 생기면 순서대로.

1. **`main.py`(CLI)와 `gui.py`의 로직 중복** — 채팅방 검색·검증이 양쪽에 복사되어 있다.
   지금은 GUI만 관리되고 있어서 `main.py`는 구버전 동작이다. 공통 모듈로 빼거나,
   CLI를 버릴 거면 지워라.
2. **친구명 매칭 기능 부재** — CSV의 이름이 카카오톡 친구명과 정확히 같아야만 동작한다.
   실제 친구명은 "김철수 어머니(3학년)" 같은 형태라 사용자가 외부 도구로 매칭하고 있다.
   앱 안에 "CSV 이름 ↔ 실제 친구명" 매칭 미리보기를 넣으면 이 프로그램의 최대 실패 지점이 사라진다.
   (발송 전 최종 확인 단계 역할도 하게 된다)
3. **이미지 구분자 쉼표 미지원** — 비개발자는 십중팔구 쉼표를 쓴다. 감지해서 안내하거나
   따옴표로 감싼 경우에 한해 허용하는 것을 검토.
4. **Windows 버전 없음** — `*_win.py` 스텁이 있었으나 미완성이라 공개 범위에서 제외했다.
   `gui.py`의 OS 분기 코드는 남아 있다.
5. **친구 삭제 앱** — `delete_gui.py`/`delete_controller.py`가 로컬에 있으나 위험해서
   공개 저장소에서 제외했다.
